# PINNED to the versions used for the live clean-account validation, recorded in evidence/.
# An independent verifier must resolve the SAME versions -- otherwise a synthesis difference cannot be
# told apart from version drift, which would make their result incomparable to the recorded evidence.
# Bump deliberately: re-run the CDK assertions and note the change in RELEASE-MANIFEST.md.
aws-cdk-lib==2.262.1
constructs==10.7.1
